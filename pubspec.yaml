name: full_screen_image
description: Full screen photo viewer. It shuts off when scrolling vertically. Can work with Hero widget.
version: 2.0.0
homepage: https://github.com/Furkankyl/full_screen_image

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
